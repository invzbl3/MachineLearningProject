package four.chapter;

/*public class MainApp extends Application {
    final XYChart.Series<String, Number> series =
            new XYChart.Series<>();
    final CategoryAxis xAxis = new CategoryAxis();
    final NumberAxis yAxis =
            new NumberAxis(8000000,
                    11000000, 1000000);
    final static String belgium =
            "Belgium";
    // ...
}*/

/**
 * In JavaFx, the main method usually launches
 * the application using the base class launch
 * method. Eventually, the start method is called,
 * which we override. In this example, we call the
 * simpleLineChart method where the user
 * interface is create:
 */